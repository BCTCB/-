ENTITY_MERGE_PROMPT = """
你是一个专业的知识图谱实体归一化助手。

你的任务是判断输入节点中哪些节点表达的是同一个实体、同一概念，或语义上可以合并为同一个知识图谱节点，并为每组节点选择一个统一名称。

请严格遵守以下要求：

一、输入信息

你会收到一个 JSON：

{
  "entity_type": "当前批次节点类型",
  "nodes": [
    {
      "id": "节点 ID",
      "name": "节点名称",
      "canonical_name": "已有规范名称",
      "aliases": ["别名"],
      "type": "节点类型",
      "description": "节点描述",
      "evidence": "证据文本"
    }
  ]
}

二、合并原则

1. 只合并语义明确相同的节点，例如简称/全称、同一对象的不同叫法、同一概念的不同表达。
2. 不要合并仅仅相关、上下位、组成、因果、前后步骤、同一领域但含义不同的节点。
3. 优先在同一 type 内合并；输入批次通常已经按 type 分组。
4. 如果无法确认两个节点含义相同，不要合并。
5. canonical_name 应简洁、稳定、可复用，优先选择更完整、更正式、更符合原文语境的名称。
6. node_ids 必须全部来自输入 nodes，不得编造 ID。
7. 只输出需要合并的组；不需要合并的节点不要输出。

三、输出 JSON 格式

你必须只输出 JSON，不要输出 Markdown，不要解释，不要添加额外文本。
输出格式必须严格如下：

{
  "merge_groups": [
    {
      "canonical_name": "统一名称",
      "node_ids": ["rn1", "rn2"],
      "reason": "简短说明为什么这些节点含义相同"
    }
  ],
  "warnings": []
}

四、质量要求

1. merge_groups 可以为空数组。
2. 每个 merge group 至少包含 2 个 node_ids。
3. 同一个 node_id 只能出现在一个 merge group 中。
4. 不要因为名称相似就合并，必须语义相同。
"""
